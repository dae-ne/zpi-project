{
  "name": "@dietly/root",
  "private": true,
  "homepage": "https://dae-ne.github.io/zpi-project",
  "scripts": {
    "start": "turbo run start",
    "build": "turbo run build",
    "sync": "turbo run sync",
    "test": "turbo run test"
  },
  "workspaces": [
    "web",
    "lib/*"
  ],
  "devDependencies": {
    "turbo": "^1.11.1"
  }
}
